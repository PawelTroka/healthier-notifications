# Working on this repository

## Purpose and preferences

Maintain a reviewable notification policy for the user's OPPO phone and Garmin watch. Desired settings and setup context are recorded in `config/policy.json` and the device guide.

The user wants calls and direct messages across all communicator apps. Work hours are exactly Monday-Friday 10:14-18:14 Europe/Warsaw. Keep personal messaging separate from work quiet hours. Clock alarms must still sound during quiet hours and sleep/DND. The user corrected the alarm setup: the original enabled Monday-Friday alarms were correct, work prep at 10:12 and work now at 10:14. Preserve both labels, times and recurrence. Moving work prep to 10:09 was a setup mistake. Do not treat the earlier 10:09 suggestion as authority to replace or add an alarm; schedule.wake_alarm is null and no separate new alarm is requested. `config/policy.json` holds the public intent. The private `.local/policy.personal.json` records installed-app choices and confirmed sleep choices; the public template leaves sleep hours unset and app rules empty. Keep that profile and `.local/native-setup.json` out of Git. The mistaken 10:09 change is retained as historical evidence; work prep was restored to 10:12 and the saved Clock list verified both original alarms enabled Monday-Friday, with work now preserved at 10:14. See docs/hardware-session.md for the correction status. A locked-phone DND sound test passed with Bluetooth off, but ran during work hours; the stricter `test.wake_alarm` work-quiet-hours condition remains pending.

## Current implementation

- Python 3.10+ standard library, launched with `python -m healthier_notifications` or `notifications.ps1`.
- The original Windows workspace has Python 3.14 and portable ADB 37.0.1 under ignored `.tools/platform-tools`. Fresh clones need Platform-Tools installed. The first connected inventory on 2026-09-15 identified an OPPO Find X9 Ultra (CPH2841), Android 16; private snapshots record the actual Android user and device identity.
- `android.py` reads ADB metadata and selected-user runtime notification permissions. `model.py` validates policy and compares state. `cli.py` implements preview/apply/restore and manual checks.
- Android whole-app permission automation is supported only when the live metadata passes the implemented checks. Channels, app schedules, Garmin allowlisting and watch-native controls are guided manual steps.
- `state/` is an intentionally exported summary. `.local/` holds sensitive inventory, serials, permission journals and human check notes; keep it out of Git. Inventory drafts use `local_only: true`, which refuses `status --export`. Run private-profile status without export; select only a reviewed public policy for shared state. Exports include every app rule and manual instruction in the selected policy.

## Continue with a connected phone

1. Read README, `docs/hardware-session.md` and the current private policy when present. Check ADB availability; inspect the authorized device and actual Android user rather than assuming hardware details.
2. The original workspace has an initial inventory in `.local/baseline.initial.json`, the latest observation in `.local/latest.json`, and current choices in `.local/policy.personal.json`. Preserve the initial baseline. Use `init` for a new setup, or `scan` to refresh an existing observation. Identify communicator/work apps from installed packages. Keep useful app notifications and tune unwanted categories at their source.
3. Use `--policy .local/policy.personal.json` before `plan`, `apply`, `checks`, `attest` and private `status` when continuing this setup. Read `.local/native-setup.json` for configuration observations and `.local/ui-actions.jsonl` for UI actions; evidence is under `.local/ui/`. Follow the user's authorization for applying specific settings. Do not infer a request to block every unknown app from “quiet by default.”
4. Complete and test the app/watch choices. Only record an attestation after the corresponding check actually happened. Garmin state is not read automatically.
5. Keep private status local. Export only the reviewed public policy, explain pending manual checks, and distinguish observed state from desired policy. Reconcile user edits deliberately; do not overwrite desired policy from a raw dump.
6. Check `.local/session-environment.json` before finishing or pausing a connected session. While `restore_required` is true, restore the recorded original `stay_on_while_plugged_in` value, verify its readback, and record completion in that local journal. The temporary keep-awake setting must not remain enabled after setup.

## Verification and change boundaries

Run `python -m unittest discover -s tests -v` for behavior changes. Use the synthetic examples for offline smoke checks. Live OPPO inventory, selected phone/watch configuration readbacks and a locked-phone DND sound test have completed. The temporary alarm was deleted, Bluetooth restored on and DND restored off. During that test the work prep alarm had mistakenly been moved from 10:12 to 10:09; retain that history and the subsequent correction separately. Garmin Connect identified fenix 9 Pro - inReach, 51mm, firmware 6.38, and reconnected after Bluetooth restoration. The phone-side selection of 19 forwarding entries was saved and verified. Calls/texts/apps vibration was on; goal alerts and morning/evening reports were turned off. All seven watch sleep days were aligned with the confirmed private phone schedule, including Saturday's extension, and read back. Sleep Focus Smart Notifications stayed on; manual system DND stayed off. A sync was requested and the device list showed Connected afterward. See the private `garmin` journal section for details. The user confirmed one incoming alert reached both phone and watch; the app and alert type were not specified. Teams/Outlook work-schedule boundary tests, Sleep Focus behavior and the broader notification tests remain pending. Automated permission writes and alarm delivery during work quiet hours remain unverified on hardware; configuration readback does not count as a delivery test.

The private policy includes `test.phone_alarm_fallback` for the completed locked-phone + DND + Bluetooth-off sound test and cleanup, and `test.incoming_alert_pair` for the single user-confirmed incoming alert on both devices. These narrower rules do not replace `test.wake_alarm` or `test.delivery`; adding them does not record attestations. Preserve all app choices when updating manual rules; a policy change makes existing attestations stale.

Seven scoped passes were recorded under the previous private policy: `phone.discord_categories`, `phone.teams_categories`, `phone.whatsapp_categories`, `phone.telegram_categories`, `phone.outlook_work_schedule`, `test.phone_alarm_fallback` and `test.incoming_alert_pair`. The alarm-policy correction makes those attestations stale. Preserve their original records and hashes; do not rewrite or automatically renew them. Refresh `.local/status.personal.md` against the corrected policy. The nine broad original rules and generated package-specific watch delivery rules remain unverified.

Outlook's selected Microsoft 365 work account has daily 18:14-10:14 quiet hours and all-day Saturday/Sunday quiet enabled, with exact settings read back. The personal Outlook.com account was untouched. Focused Inbox only mail notifications were preserved, and Set on Teams and Outlook remained off. Calendar reminders are separate from mail quiet time; practical work-schedule boundary tests remain pending. Keep account identifiers private.

WhatsApp Android categories Group notifications, Chat history backup and Group join requests were disabled and read back; Message notifications remained enabled. Telegram's global Groups, Channels, Reactions and Stories, Contact joined Telegram and Pinned Messages were off after setup, with Private Chats enabled and call ringtone/vibration preserved. The two Groups and two Channels notification exceptions were removed and their absence verified; no chats or messages were deleted. Repeat Notifications was set to Never. These are configuration observations, separate from the one user-confirmed incoming alert.

The completed Android category pass disabled 33 categories in total, including selected Instagram, LinkedIn, Discord and Picsart activity/promotional categories. Discord's Messages category was disabled under Server; Direct messages and Incoming calls remained enabled. Mixed/unknown source categories were preserved, so direct-message-only behavior remains incomplete for some apps. Meetup exposed no separate direct-message category. Read the private `source_category_reviews` and `channels` journals for exact choices and limitations. No whole-app permission changes were made; the refreshed inventory's selected permission records matched the initial baseline.

Session cleanup restored `stay_on_while_plugged_in` to its original value 0 and verified it; `.local/session-environment.json` now has `restore_required: false`. Bluetooth was on, DND off and alarm volume 12 at final readback. Normal screen timeout was restored and the phone returned home.

Preserve write-ahead journals, per-user parsing, shared-UID/system/role protections, live preflight and readback verification. Never interpret unknown parser output as denied/allowed. Device/firmware differences should produce an actionable manual path. Keep permission grants distinct from channel alert behavior and watch delivery. Do not collect notification text or private app databases to obtain configuration state.

Primary capability references and menu/version caveats are in `docs/sources.md` and `docs/device-guide.md`.

## Remaining-noise follow-up, 2026-09-18

User photos show residual automated/promotional notifications. Keep those uploads private under ignored `.codex-remote-attachments/`; never publish their notification contents. The private `.local/follow-up-2026-09-18.json` contains the source-by-source review, protected types and pending routine-update choice. Human-sent reels count as direct messages. Garmin Connect's own Push Notifications are separate from watch forwarding and native watch alerts; its silent sync-service card does not establish an interruption. Allegro needs source filters while preserving seller/buyer direct messages. Inspect in-app controls for mixed categories.

The phone was connected but locked at the last settings-access check. No additional notification settings were changed in this follow-up, and temporary keep-awake was not enabled. A fresh permission scan at 2026-09-18 18:03:39 UTC found all 203 selected permission records unchanged from the initial baseline. The private policy adds two pending follow-up rules and targeted notes without altering app-level choices or alarms; private/public status reports were refreshed. Preserve all historical attestations. Continue after physical unlock and resolve routine-update presentation from the user's pending answer; do not assume every visible card sounded or vibrated.

The second photo batch includes phone screenshots dated September 16; it is earlier evidence, not a fresh delivery test. The private follow-up now has 24 items, including work-email relevance within work hours and silent companion-service cards. Preserve appointment reminders and actionable warnings even when automatically generated. Work email volume within work hours does not establish schedule failure. Account/source-subscription changes are a separate review; no mailbox or subscription settings have been changed.
